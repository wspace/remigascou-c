#include <stdio.h>

/*
 * This is a WhiteSpace interpreter
 */

int main(int argc, char const *argv[]) {
    if (argc != 2) {
        printf("Usage : %s program.ws\n", argv[0]);
    } else {
        
    }
    return 0;
}
